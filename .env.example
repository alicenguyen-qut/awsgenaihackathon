# AWS Configuration (Optional - Makefile commands override these)
# When using 'make run-local' or 'make run-aws', these are ignored
# Only needed when running directly: python src/app.py
USE_AWS=false
AWS_REGION=ap-southeast-2
RECIPES_BUCKET=

# Flask Configuration
SECRET_KEY=dev-secret-key-change-in-production
